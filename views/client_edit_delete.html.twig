<!-- client -->
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Urbaca Salon</title>
    </head>
    <body>
        <div class="container">
<!--Header-->
            <div class="page-header">
                <h1>Urbaca Salon</h1>
            </div>
            <div class="row">
<!-- Edit Stylist Info-->
                <div class="col-md-6">
                    <h2>Update Info for {{ client.getClient }}</h2>
                    <form class="" action="/client/update/{{ client.getId }}" method="post">
                        <label for="name">Full Name:</label>
                        <input type="text" name="name" value="{{ client.getClient }}">
                        <label for="phone">Phone Contact:</label>
                        <input type="number" name="phone" value="{{ client.getNumber }}">
                        <button type="submit" name="button">Edit Client Info</button>
                    </form>
                </div>
                <br>
<!-- Delete Stylist -->
                <div class="col-md-6">
                    <form class="" action="/client/delete/{{ client.getId }}" method="post">
                        <button type="submit" name="button">Delete Client</button>
                    </form>
<!--Back Button -->
                    <form class="" action="/client/{{ client.getId }}" method="get">
                        <button type="submit">Back</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
